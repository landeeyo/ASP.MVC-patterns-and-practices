@model PassingModels.Models.ViewModels.SearchVM

Filter:
<br />


@using (Html.BeginForm(MVC.Search.ActionNames.Search, MVC.Search.Name, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.HiddenFor(model => model.Setting)
    @Html.TextBoxFor(model => model.Query)
    <input type="submit" value="Search" />
}

@*@using (Html.BeginForm(MVC.Search.ActionNames.Search, MVC.Search.Name, new { SearchVM = Model }, FormMethod.Post))*@